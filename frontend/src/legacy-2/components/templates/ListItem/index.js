import React from 'react';

import { FiImage } from 'react-icons/fi';

import './styles.css';

export default props => {
  const {
    title,
    img,
    content,
    footer
  } = props.config;

  return(
    <li className="item">
      
    </li>
  );
};